namespace EHealth.Lab.Data;

public static class Seeder
{
    public static void Seed(AppDbContext db)
    {
        // No lab results are seeded — they are entered live during the demo (Lab app),
        // each published to the DKG at that point.
    }
}
